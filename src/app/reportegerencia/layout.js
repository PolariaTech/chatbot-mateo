export const metadata = {
  title: 'Reporte gerencia - Polaria Mateo',
  description: 'Tablero de indicadores por SKU',
};

export default function ReporteGerenciaLayout({ children }) {
  return children;
}
